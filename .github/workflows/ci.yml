name: ISpooferMotion CI

on:
  workflow_call:
    inputs:
      source_sha:
        type: string
        required: true

permissions:
  contents: read

concurrency:
  group: ism-ci-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust-platforms:
    name: Rust (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-15, ubuntu-22.04]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: ISpooferMotion/Proxy
          path: automation
          persist-credentials: false
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.source_sha }}
          path: source
          persist-credentials: false
      - uses: ./automation/.github/actions/setup-build
        with:
          cache-key: ci-platform-${{ matrix.os }}
          components: clippy,rustfmt
          desktop: "true"
      - if: runner.os == 'Linux'
        working-directory: source
        run: cargo fmt --all -- --check
      - working-directory: source
        run: cargo check --locked -p daemon -p ui --all-targets --all-features
      - working-directory: source
        run: cargo test --locked -p daemon -p ui -p ism-auth -p ism-config -p ism-security -p ism-updater --all-features --no-fail-fast
      - working-directory: source
        run: cargo clippy --locked -p daemon -p ui -p ism-auth -p ism-config -p ism-security -p ism-updater --all-targets --all-features -- -D warnings

  windows-workspace:
    name: Full Windows workspace
    runs-on: windows-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: ISpooferMotion/Proxy
          path: automation
          persist-credentials: false
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.source_sha }}
          path: source
          persist-credentials: false
      - uses: ./automation/.github/actions/setup-build
        with:
          cache-key: ci-windows-workspace
          components: clippy
          desktop: "false"
      - working-directory: source
        run: cargo check --locked --workspace --all-targets --all-features
      - working-directory: source
        run: cargo test --locked --workspace --all-features --no-fail-fast
      - working-directory: source
        run: cargo clippy --locked --workspace --all-targets --all-features -- -D warnings

  obfuscator:
    name: Obfuscator quality
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: ISpooferMotion/Proxy
          path: automation
          persist-credentials: false
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.source_sha }}
          path: source
          persist-credentials: false
      - uses: ./automation/.github/actions/setup-build
        with:
          cache-key: ci-obfuscator
          components: clippy
          desktop: "false"
      - working-directory: source
        run: cargo test --locked -p obfuscator --all-targets --no-fail-fast
      - working-directory: source
        run: cargo clippy --locked -p obfuscator --all-targets --all-features -- -D warnings

  typescript:
    name: UI and release contracts
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: ISpooferMotion/Proxy
          path: automation
          persist-credentials: false
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: ${{ inputs.source_sha }}
          path: source
          persist-credentials: false
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          repository: ISpooferMotion/Core
          ref: 05264ea85745b9041a01c79d04b986cdaf426787
          path: Core
          persist-credentials: false
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.14
      - name: Build pinned Core package
        working-directory: Core
        run: |
          bun install --frozen-lockfile --ignore-scripts
          bun run build
          bun run package:pack
          cp .artifacts/package.tgz ispoofermotion-core-4.1.1.tgz
      - name: Install locked dependencies
        run: |
          cd source
          bun install --frozen-lockfile --ignore-scripts
          cd apps/ui
          bun install --frozen-lockfile --ignore-scripts
      - working-directory: source
        run: bun run typecheck
      - run: node automation/scripts/test-release-tools.mjs
      - working-directory: source
        run: bun x biome ci ./
      - working-directory: source
        run: bun test scripts/test/test-project-contracts.test.ts scripts/test/test-version-sync.test.ts scripts/test/test-release-tools.test.ts
      - working-directory: source/apps/ui
        run: bun run lint
      - working-directory: source/apps/ui
        run: bun run build
